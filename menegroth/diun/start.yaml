- name: Start Diun
  hosts: Menegroth
  tasks:
    - name: Start Diun Docker container
      become: true
      become_user: ashish
      community.docker.docker_container:
        name: diun
        image: crazymax/diun:4.33.0
        labels:
          diun.enable: 'true'
          diun.watch_repo: 'true'
          diun.notify_on: new;update
          diun.sort_tags: semver
          diun.max_tags: '1'
          diun.include_tags: '^\d+\.\d+\.\d+$'
        command: 'serve --config /config/diun.yaml'
        restart_policy: unless-stopped
        hostname: '{{ansible_facts["hostname"]}}'
        volumes:
          - /System/Volumes/Data/data/diun.yaml:/config/diun.yaml:ro
          - diun-data:/data
          - /var/run/docker.sock:/var/run/docker.sock
          - /System/Volumes/Data/data/certs/homelab_ca.crt:/etc/ssl/certs/homelab_ca.crt:ro
          - /etc/localtime:/etc/localtime:ro
        keep_volumes: false
        state: started
